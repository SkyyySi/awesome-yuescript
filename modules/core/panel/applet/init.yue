const submodules = { "battery", "brightness", "clock", "dock", "keyboard_layout", "tag_add_remove", "taglist", "tasklist", "wifi" }
export default { name, require(... .. "." .. name) for name in *submodules }
